import "jest-styled-components";

import React from "react";

import { shallow } from "enzyme";

import App from "../../../../pages/_app.js";

describe("`_app.js` implementation tests", () => {
  it("", () => {
    const app = shallow(<App />);
  });
});
